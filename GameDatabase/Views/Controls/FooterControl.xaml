<UserControl x:Class="GameDatabase.FooterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GameDatabase"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Bottom" Height="20" Background="#EEE" >
        <Border BorderThickness="0,1,0,1" BorderBrush="Navy">
            <TextBlock Text="{Binding Path=FooterContent}" Width="{Binding Path=FooterWidth}" TextAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
